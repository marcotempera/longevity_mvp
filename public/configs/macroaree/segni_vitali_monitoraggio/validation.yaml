macroarea: segni_vitali_monitoraggio
version: 1.1

rules:
  - feature: pressione_domiciliare
    rule: "if includes 'no_mai' then set to ['no_mai']"

  - feature: aritmie_percepite
    rule: "if includes 'mai' then set to ['mai']"

  - feature: termoregolazione
    rule: "if includes 'mai' then set to ['mai']"

  - feature: wearable_monitoring
    rule: "if includes 'mai' then set to ['mai']"

  - feature: variazioni_peso
    rule: "if includes 'no' then set to ['no']"

  - feature: svenimenti
    rule: "if includes 'mai' then set to ['mai']"

  - feature: fc_riposo
    rule: "if includes 'non_misurato' then set to ['non_misurato']"

  - feature: respiro_riposo
    rule: "if includes 'regolare' then set to ['regolare']"

  - feature: spo2
    rule: "if includes 'mai' then set to ['mai']"

  - feature: termoregolazione_alterata
    rule: "if includes 'mai' then set to ['mai']"

missing:
  strategy: "per_feature_policy"